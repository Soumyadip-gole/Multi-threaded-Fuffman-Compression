# Multi-threaded-Fuffman-Compression
A pure Rust-based multi-threaded Huffman encoder and decoder, using a custom thread pool to compress and decompress multiple files in parallel with safe and efficient concurrency.
